# My Vite + Tailwind App

Quick scaffold for a Vite + React + TypeScript + Tailwind CSS app.

Try it:

```bash
# install deps
npm install

# start dev server
npm run dev
```

Build:

```bash
npm run build
npm run preview
```
